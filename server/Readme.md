- to install deps:

```
npm install
```

- setup the .env file:

PORT = 5173

MODE = development

DB_HOST=localhost
DB_DATABASE=smqnorm
DB_USERNAME=
DB_PASSWORD=

- run the app at /server:

```
nodemon index.js
```

